export function translateKick(isDanish: boolean): string {
  return isDanish ? "Smid ud" : "Kick";
}

export function translateCrown(isDanish: boolean): string {
  return isDanish ? "Kron" : "Crown";
}
