export function translateEditLobbyName(isDanish: boolean): string {
  return isDanish ? "Redigér spilnavn" : "Edit lobby name";
}

export function translatePrivate(isDanish: boolean): string {
  return isDanish ? "Privat" : "Private";
}

export function translatePublic(isDanish: boolean): string {
  return isDanish ? "Offentlig" : "Public";
}
