export function translatePlayAgain(isDanish: boolean): string {
  return isDanish ? "Spil igen" : "Play again";
}

export function translateEndGame(isDanish: boolean): string {
  return isDanish ? "Afslut spil" : "End game";
}
